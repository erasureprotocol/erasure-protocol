{
  "name": "erasure",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ jgeary/erasure",
    "create-local": "graph create --node http://localhost:8020/ jgeary/erasure",
    "remove-local": "graph remove --node http://localhost:8020/ jgeary/erasure",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jgeary/erasure"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.0",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
